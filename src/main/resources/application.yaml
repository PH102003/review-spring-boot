spring:
  application:
    name: parking-ctrl
  datasource:
    url: jdbc:postgresql://localhost:5432/parking-ctrl-db
    username: postgres
    password: Tu@irm@5@f@d@
    driver-class-name: org.postgresql.Driver
  # JPA / Hibernate Configuration
  # vou deixar update, mas em produção é bom considerar o 'validate' 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        '[format_sql]': true